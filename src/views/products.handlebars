<div>

  <h3>Productos</h3>
{{#if cartId}}
  	<a href="/carts/{{cartId}}"><button>Ver Carrito</button></a>
    <p id="cartId">{{cartId}}</p>
{{/if}}

      {{!-- <input type="button" value="Crear carrito" id="createCart" />	 --}}
{{#unless cartId}}
      <button id="createCart" >Crear carrito</button>
{{/unless}}


<div>
  <p>Ordenar por:</p>
	<a href="/products?page={{page}}&sort=asc"><button>Mayor precio</button></a>
	<a href="/products?page={{page}}&sort=desc"><button>Menor precio</button></a>

</div>

<div>
  {{#unless cat}}
  <p>Filtrar por:</p>
	<a href="/products?page={{page}}&cat=fijación"><button>Fijación</button></a>
	<a href="/products?page={{page}}&cat=herramientas"><button>Herramientas</button></a>
{{/unless}}

{{#if cat}}
  <p>Filtrado por {{cat}}</p>
	<a href="/products?page={{page}}"><button>Eliminar filtro</button></a><br>
{{/if}}

</div>
<input type="text" id="prodId">
<input type="button" value="Agregar al carrito" id="addToCart" />

	{{#each productsDocs}}
    <div >
      <p>Title: {{this.title}}</p>
      <p>Description: {{this.description}}</p>
      <p>Code: {{this.code}}</p>
      <p>Price: ${{this.price}}</p>
      <p>Stock: {{this.stock}}</p>
      <p>Category: {{this.category}}</p>
      <p>Id: {{this.prodId}}</p>
      <input type="button" value="Agregar al carrito" />
    	 {{!-- <a href="/products?page={{page}}&sort=desc"><button id=addToCart>Agregar al carrito</button></a> --}}
      <p>-----------------------------------------</p>

    </div>

	{{/each}}


{{#if hasPrevPage}}
	<a href="/products?page={{prevPage}}"><button>Página anterior</button></a>
{{/if}}
{{#if hasNextPage}}
	<a href="/products?page={{nextPage}}"><button>Siguiente página</button></a>
{{/if}}
</div>


<script src='/js/products.js'></script>
